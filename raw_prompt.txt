❯ pertahankan generic, cek fungsi admin_mount.

❯ DONT ALWAYS REREAD FILE. KEEP EFFICIENT. DONT WASTE TOKEN. CODE AS GENERIC, SHORT, SIMPLE AND BEAUTIFUL AS POSSIBLE.

now we must make journal more robust since it main source of all report. auto journal added from invoice cannot be edited directly from journal form. it can but troough the invoice
which link to journal. and it value/amount must always the sama as invoice (relation -> get value) and auto balancing (eg: trasaction 20.000 -> custumer pay cash 10.000 -> journal
child table: baris 1 cash = 10000 (debit), baris 2 piutang = 10000 (debit) baris 3 penjualan = 20000 (credit).

jurnal yang diinput manual tetap bisa diedit manual

future -> edit docs ini nanti hanya berdasarkan role


buat field auto terisi jika ada relasi. misal: saat input sales, penambahan barang dan input barang, seharusnya satuan default untuk sales
dan harga (yang lainnya yang terelasi otomatis terisi. buat di funsi generic seperti ini auto terisi. pada setiap child table adakan tombol juga untuk custom field apa saja yang mau ditampilan user

begitu juga pada semua form, jika company hanya 1 maka seluruh field di form auto mengisi company. jika company lebih dari 1 maka user memilih (checkbox default=true) maka ini yang
akan menjadi default. default company ini juga bisa di set per user (user company apa)

pada list view pada tombol column, munculkan semua kolom yang ada (default) agar user bisa memilih kolom apa saja yang ditampilkan. juga ada settingan di custumize pag. e. settingan
ini harus selalu sama antara list view dan cuztumize (simpan di db).

perbaiki nilai return dari list view karena kolom company muncul segbagai "1" (id) bukan nama company.

tampilan pos sudah di update. jangan dirubah.

stock entri tidak ada di menu. apakah sudah dibuat untuk stock entry? -> ini juga akan seperti journal. terisi otomatis (dari purchase atau sales) tetapi bisa manual juga.


JANGAN MENJAWAB SETIAP TASK. LAPORAN SAJA DIAKHIR DENGAN SINGKAT. DONT WASTE TOKEN

❯ we can achive by simple 1 charge table. it can be anythung charge. make amount is optional between fix number or percentage.
. usualy this is for sales. since purchase not paid by us (but still musthave same compatibility). user can add what charge
included in sales/purchase company default or doc setting, or in sales/purchase inv directly. 1 sales/purchace can have more 1 charge eg: tax, service. and this is auto count. in pos too. dont re read, you already know the code, nothing change from your last read. dont waste token. keep code simple

❯ check if all data load follow aras framework standard
❯ is there REST API endpoints call to framework with have own function/override (not from framework?)

❯ there is system hidden field loaded in dnd editor. this should follows mgr_colums show_in_form its hidden or not eg: created_at, created_by_id, etc. so dnd will show the exact same field as form


❯ - delete charge category (already deleted). check -> pos, sales, purchase will use directly from charge use charge for company that support more than 1 so its child table. it should not show in menu but in company form. - merge payment type to mode of payment, and we add child table to support multi company and coa fild for each company in there. - we dont need pos order since POS just view to store transaction directly to invoice. - and shift report will have opening, closing balance for every mode of payment(auto count), and balance for every mode of payment (manual input) - create option for item that price will use default field or from item price table (support multi level price for both sell and buy) - use check box (Use Price Table) -

❯ test transaction item (is stock) -> set price in table not directly in field -> input purchase (stock must added) -> sell trough pos -> check sales invoice + journal -> cek report. make cli command too for this with auto seed data and support number of row parameter inpu eg: flask aras erp test...

❯ create stock report. and add number of current stock to view in product page (in right box/ detail box" - not in tab)

❯ now create 1 cli command that include all transaction flow from seed to report

❯ - remember my new css is in static/css/aras-design. - resolve all counted/formula field to be automated count and not editable manually. mark for table that need count view can be set what to view below eg: AccSalesInvoiceLine can show total in bottom of child list view and what user whant like charges (tax, service) - we have to delete amount paid from sales invoice since we have to store it in table and sales invoice linked to that table (support multi payment and credit payment) - product type in product table can be deleted since we have product category. to simplify, cost price and standard price can be deleted too. we will only use price from price table OR use cost price field for calculation cost price base on (everu purchace price in purchase inv *qty)/qty (must be this way because purchase row can be in different price. - code / barcode, pick one to keep, delete one. seems delete barcode much easier - now we have can support tab in form view and support customizing view for form view. improve this. think about some like parameter from mgr_column if child_table AND view_in_tab=1 then auto load it to new tab. tab title is the same as child list title - in every list view (generic) see _List_partial / macro user can always show the column their pick. it available in main list view but not child table. but in main list view its not work bacause not al data loaded. keep generic so all can implement from generic function - in main list view filter see _List_partial / macro it must have ability to filter into child table eg: sales invoice list view can filter by product inside it - add in COA is_group so if its group, it just calculate the child COA. FOLLOW ARAS FRAMEWORK RULES. IF NEED CISTUMIZING IN ARASFRAMEWORK, IT MUST BE GENERIC. DONT READ THE WHOLE FILES, ONLY DIFF. USE smart_read.sh to read files. if in the work you find something you can refactor, refactor in. MUST DRY (Don't Repeat Yourself) principles. if many function writen, create the generic one in arasCore/lib
